title: post
"@extends": default

form:
  fields:
    tabs:
      fields:
        content:
          fields:

            header.image:
              type: pagemediaselect
              label: image
              style: vertical
              ordering@: header.display_title
              accept:
                - jpg
                - gif

            header.image_alt:
              type: text
              label: image_alt
              style: vertical
              ordering@: header.image
