title: emphasis
'@extends': modular/module

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:

        content:
          type: tab
          title: Content
          fields:

            header.color_bg:
              type: colorpicker
              label: color_bg

            header.color_fg:
              type: colorpicker
              label: color_fg

            header.color_btn_bg:
              type: colorpicker
              label: color_btn_bg

            header.color_btn_fg:
              type: colorpicker
              label: color_btn_fg
