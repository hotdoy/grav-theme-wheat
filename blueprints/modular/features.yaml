title: features
extends@: modular/module

form:
  fields:
    tabs:
      fields:
        content:
          fields:
            content:
              unset@: true
            header.media_order:
              ordering@: +99

            header.items:
              name: items
              type: list
              style: vertical
              label: items
              fields:

                .image:
                  type: pagemediaselect
                  label: image

                .title:
                  type: text
                  label: title

                .text:
                  type: textarea
                  label: text
