{% extends 'partials/base.html.twig' %}

{% block metadata %}
	{{ parent() }}
	{% include "partials/event-schema.html.twig" %}
{% endblock %}

{% block content %}

	<section id="event-heading">
		<div class="wrapper text">
			<h1>{{ page.header.display_title }}</h1>
		</div>
	</section>

	[TODO]

	{# MODULES #}
	{% for module in page.collection({ items: {'@self.modular':''}, 'order': {'by':'folder', 'dir':'asc'}}) %}
	{{ module.content|raw }}
	{% endfor %}

{% endblock %}
