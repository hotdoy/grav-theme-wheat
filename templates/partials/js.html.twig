{% do assets.addJs('theme://js/performance.js', { group:'top' }) %}
{% do assets.addJs('theme://js/htmx.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/error.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/lite-yt-embed.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/link.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/img.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/scroller.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/share.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/intersect.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/fathom.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/colorscheme.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/scroll.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/product.js', { group:'top', loading: 'defer' }) %}
{% do assets.addJs('theme://js/custom.js', { group:'top', loading: 'defer' }) %}
