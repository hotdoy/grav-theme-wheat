{% do assets.addJs('theme://js/performance.js', { group:'head' }) %}

{% do assets.addJs('theme://js/htmx.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/error.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/lite-yt-embed.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/link.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/img.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/scroller.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/share.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/intersect.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/fathom.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/colorscheme.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/scroll.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/product.js', { group:'head-defered' }) %}
{% do assets.addJs('theme://js/custom.js', { group:'head-defered' }) %}
