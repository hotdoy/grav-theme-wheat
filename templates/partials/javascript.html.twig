{# LIBS #}
{% if theme.snipcart_enabled and theme.snipcart_public_key %}{% do assets.addJs('https://cdn.snipcart.com/themes/v3.0.25/default/snipcart.js', { group:'bottom' }) %}{% endif %}
{% do assets.addJs('theme://libs/swiper/swiper.min.js', { group:'bottom' }) %}

{# COMPONENTS #}
{% do assets.addJs('theme://js/reveal.js', { group:'bottom' }) %}
{% do assets.addJs('theme://js/sentinel.js', { group:'bottom' }) %}
{% do assets.addJs('theme://js/links.js', { group:'bottom'}) %}
{% do assets.addJs('theme://js/header.js', { group:'bottom' }) %}
{# {% do assets.addJs('theme://js/announcement.js', { group:'bottom' }) %} #}
{% do assets.addJs('theme://js/youtube.js', { group:'bottom' }) %}
{# {% do assets.addJs('theme://js/share.js', { group:'bottom' }) %} #}
{% do assets.addJs('theme://js/pagination.js', { group:'bottom' }) %}
{% if theme.snipcart_enabled and theme.snipcart_public_key %}{% do assets.addJs('theme://js/snipcart.js', { group:'bottom' }) %}{% endif %}

{# MODULES #}
{% do assets.addJs('theme://js/quotes.js', { group:'bottom' }) %}
{% do assets.addJs('theme://js/carousel.js', { group:'bottom' }) %}
{% do assets.addJs('theme://js/map.js', { group:'bottom' }) %}

{# PAGES #}
{% do assets.addJs('theme://js/product.js', { group:'bottom' }) %}

{# LOADING #}
{% do assets.addJs('theme://js/loading.js', { group:'bottom', priority: 0 }) %}

{# CUSTOM #}
{% do assets.addJs('theme://js/custom.js', { group:'bottom' }) %}