{% set image = image ?? null %}
{% set alt = alt ?? null %}
{% set title = title ?? null %}
{% set width = image ? image.width : null %}
{% set height = image ? image.height : null %}
{% set type =  image ? image.url|split('.')|last : null %}
{% set system_quality = system.images.default_image_quality %}
{% set quality = image.quality > system_quality ? system_quality : image.quality %}

{% if image %}
{{ dump(image) }}
{{ image.attribute('width', width).attribute('height', height).quality(quality).loading.html(title, alt)|raw }}
{% endif %}
