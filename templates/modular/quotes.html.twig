{% extends 'modular/module.html.twig' %}
{% set name = 'quotes' %}

{% block content %}
<div class="wrapper">
	<div class="scroller">
		<div class="scroller__scrollctn">
			{% for item in page.header.items %}
			{% set image = item.image ? page.media[item.image] %}
			<div class="scroller__slide dark">
				<div class="quote text">
					<blockquote>{{ item.quote }}</blockquote>
				</div>
				{% if image %}
				<div class="portrait">
					{% include "partials/img.html.twig" with {image: image} only %}
				</div>
				{% endif %}
				<div class="source text small">{{ item.source }}</div>
			</div>
			{% endfor %}
		</div>
		{% if page.header.items|length > 1 %}
		<span class="scroller__prev"></span>
		<span class="scroller__next"></span>
		{% endif %}
	</div>	
</div>
{% endblock %}