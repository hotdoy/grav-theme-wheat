{% set optional_classes = optional_classes|default(page.header.optional_classes) %}
{% set content = page.content|raw %}

<section class="module content text {{ optional_classes }}" data-reveal>
	{{ content|raw }}
</section>
