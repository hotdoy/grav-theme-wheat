document.addEventListener('appInteractive', () => {
    setTimeout(() => {

        // MODULE (DEFAULT)
        // ScrollOut({
        //     targets: ['.module'],
        //     once: true
        // });

        // NUMBERS
        // Splitting({
        //     target: ['.module.numbers .item h3'],
        // });
 
    }, App.fxDelay);
});
